use log::info;
use std::error::Error;
use gtk4 as gtk;
use gtk4::prelude::*;

pub struct ApplyImageView {
	pub container: gtk::Box,
}

impl ApplyImageView {
	pub fn new(image_id: String) -> Self {
		Self {
			
		}
	}
}